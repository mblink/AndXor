@import andxor.syntax._

@(tpes: List[String])

  def build[@{tpes.tpeParams}](implicit @{tpes.paramSig("AndXorConst", "l")}): @{tpes.builtAndXor} = @{tpes.builtAndXor}
  def buildNested[@{tpes.rank2TpeParams}](implicit @{tpes.paramSig("AndXorRank2", "l")}): @{tpes.builtAndXor} = @{tpes.builtAndXor}
