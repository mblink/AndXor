@import andxor.syntax._

@(tpes: List[String])

  def build[@{tpes.tpeParams}]: @{tpes.builtAndXor("")} = @{tpes.builtAndXor("")}
  def buildNested[@{tpes.rank2TpeParams}]: @{tpes.builtAndXor("Nested")} = @{tpes.builtAndXor("Nested")}
